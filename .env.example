# WebSocket base URL for the streaming chat (no trailing slash)
VITE_WS_URL=ws://localhost:8000

# REST API base URL (used by MonitoringPage /ready poll)
VITE_API_URL=http://localhost:8000

# Grafana dashboard base URL (embedded as iframe in MonitoringPage)
VITE_GRAFANA_URL=http://localhost:3000
